from setuptools import setup

setup(
    name="graphql",
    version="1.0.0",
    install_requires=[
        "flask",
        "flask_graphql",
        "flask_cors",
        "graphql-core",
        "graphene",
        "requests"
    ]
)
